import { LoginCredentialsDto } from './login-credentials-dto';

describe('LoginCredentialsDto', () => {
  it('should create an instance', () => {
    expect(new LoginCredentialsDto()).toBeTruthy();
  });
});
